#include <stdio.h>
/**
 * main - Entry point
 * Description - Function that prints the alphabet
 * Return: 0 Always (success)
 */
int main(void)
{
char g;

for (g = 'a'; g <= 'z'; g++)
{
putchar(g);
}
{
putchar('\n');
}

return (0);
}
