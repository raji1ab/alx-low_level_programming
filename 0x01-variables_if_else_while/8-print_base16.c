#include <stdio.h>
/**
 * main - Entry point
 * Description - hexadecimal
 * Return: Always 0 (success)
 */
int main(void)
{
	char x;

for (x = 48; x <= 57; x++)
{
	putchar(x);
}
for (x = 97; x <= 102; x++)
{
	putchar(x);
}
{
	putchar('\n');
}
return (0);
}
