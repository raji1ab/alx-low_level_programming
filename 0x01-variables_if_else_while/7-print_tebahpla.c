#include <stdio.h>
/**
 * main - Entry point
 * Description - reverse abc
 * Return: Always 0 (success)
 */
int main(void)
{
	char h;

for (h = 'z'; h >= 'a'; --h)
{
	putchar(h);
}
{
	putchar('\n');
}
return (0);
}
